export interface ParamsCreateProduct {
  name: string;
  category_id: string;
  cost_price: number;
  price: number;
  amount: number;
  description: string;
  images: string;
  unit: string;
  properties: string;
}
